三峰整装财务系统腾讯云部署信息模板

请在腾讯云开通服务器后填写以下信息。涉及密码或私钥时，可以只告诉我你准备使用哪种登录方式；敏感内容建议你在本机操作时输入，不要公开发送。

1. 服务器基础信息
服务器公网 IP：
SSH 用户名：root
SSH 端口：22
操作系统/镜像：Node.js 应用模板 / Ubuntu / 其他：

2. SSH 登录方式
登录方式：密码 / SSH 私钥
私钥文件路径（如使用私钥，建议放 D:\workspace 下）：
是否已确认本机可以 SSH 登录：是 / 否

3. 腾讯云防火墙
22/TCP 是否已放通：是 / 否
8787/TCP 是否已放通：是 / 否
80/TCP 是否已放通（HTTPS 需要）：是 / 否
443/TCP 是否已放通（HTTPS 需要）：是 / 否

4. 域名与 HTTPS（可选）
是否配置域名：是 / 否
域名：
域名是否已解析到服务器公网 IP：是 / 否
证书邮箱：

5. 客户端填写地址
临时 HTTP 地址：http://服务器公网IP:8787
正式 HTTPS 地址：https://你的域名

6. 部署命令参考
无域名：
powershell -ExecutionPolicy Bypass -File .\cloud-api\deploy\deploy-full-from-windows.ps1 -HostName 服务器IP -User root

使用私钥：
powershell -ExecutionPolicy Bypass -File .\cloud-api\deploy\deploy-full-from-windows.ps1 -HostName 服务器IP -User root -SshKeyPath D:\workspace\your-key.pem

带 HTTPS：
powershell -ExecutionPolicy Bypass -File .\cloud-api\deploy\deploy-full-from-windows.ps1 -HostName 服务器IP -User root -Domain api.example.com -Email admin@example.com
